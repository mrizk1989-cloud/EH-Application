<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>E&H Request Form</title>

    <link rel="stylesheet" href="/css/UserForm.css">
</head>

<body>

<div class="container">

    <h2>User Request Form</h2>

    <form id="requestForm" enctype="multipart/form-data">

        <!-- CSRF TOKEN -->
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">

        <table id="requestTable">

            <thead>
                <tr>
                    <th>Select</th>
                    <th>Customer ID</th>
                    <th>Amount </th>
                    <th>Currency</th>
                    <th>Expense Type</th>
                    <th>Purpose</th>
                    <th>Doctor</th>
                    <th>Month</th>
                    <th>Year</th>
                </tr>
            </thead>

            <tbody id="tableBody">

                <tr>
                    <td><input type="checkbox" class="row-check"></td>

                    <td><input type="text" name="customerId[]" required></td>

                    <td><input type="text" name="amount[]" class="amount-input"
                                inputmode="decimal" pattern="^\d+(\.\d{1,2})?$" required></td>

                    <td>
                        <select name="currency[]">
                            <option value="USD">USD</option>
                            <option value="SAR">SAR</option>
                            <option value="EUR">EUR</option>
                        </select>
                    </td>

                    <td>
                        <select name="expenseType[]">
                            <option value="medical">Medical</option>
                            <option value="travel">Travel</option>
                            <option value="other">Other</option>
                        </select>
                    </td>

                    <td><input type="text" name="purpose[]"></td>
                    <td><input type="text" name="doctor[]"></td>

                    <td>
                        <select name="month[]">
                            <% for(let i=1;i<=12;i++){ %>
                                <option value="<%= i %>"><%= i %></option>
                            <% } %>
                        </select>
                    </td>

                    <td>
                        <select name="year[]">
                            <% for(let y=2020;y<=2030;y++){ %>
                                <option value="<%= y %>"><%= y %></option>
                            <% } %>
                        </select>
                    </td>

                </tr>

            </tbody>
        </table>

        <!-- FILE UPLOAD -->
        <div class="file-box">
            <label>
                Attach Files:
                <input type="file" name="files" multiple>
            </label>
        </div>

        <!-- ACTION BUTTONS (NO INLINE ACTIONS) -->
        <div class="actions">

            <button type="button" id="addRowBtn">Add New Line</button>

            <button type="button" id="deleteRowsBtn">Delete Selected</button>

            <button type="submit">Submit Request</button>

        </div>

    </form>

</div>

<!-- EXTERNAL JS ONLY -->
<script src="/js/requestForm.js"></script>


</body>
</html>